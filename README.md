# 📍 Atividade Técnica: Integração de API e Automação de Formulário

Este repositório registra a resolução de uma atividade técnica focada na integração entre **React** e **Programação Orientada a Objetos (POO)**. O objetivo principal foi implementar a busca automática de endereços via API para otimizar o fluxo de cadastro.

### 📝 Descrição da Atividade
O desafio consistiu em adaptar códigos base para implementar a busca automática de informações de endereço com base no CEP fornecido. A solução une a base teórica de classes JavaScript com a prática de componentes funcionais em React.

### 🎯 Requisitos Implementados
* **Busca Automática:** Integração com a API ViaCEP para preenchimento instantâneo de dados.
* **Validação de Input:** Controle de campo para permitir apenas números e limite de 8 caracteres (maxLength).
* **Tratamento de Eventos:** Uso de `onChange` para disparar a consulta assim que o CEP é preenchido.
* **Adaptação de Código:** Ajuste e integração entre os arquivos de lógica de objeto (`Endereco.mjs`) e interface (`EnderecoForm.jsx`).


### 🧰 Tecnologias e Conceitos
* **ReactJS:** Gestão de estados e interface.
* **JavaScript (POO):** Manipulação de objetos de negócio.
* **Fetch API:** Consumo de serviços externos em tempo real.
